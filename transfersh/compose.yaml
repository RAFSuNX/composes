services:
  transfer:
    image: dutchcoders/transfer.sh:latest
    hostname: transfersh
    command: --provider local --basedir /tmp/
    environment:
      TZ: ${TZ}
    volumes:
      - transfer_data:/tmp/
    networks:
      cloudflared:
        aliases:
          - transfersh
    restart: on-failure

volumes:
  transfer_data:

networks:
  cloudflared:
    external: true
