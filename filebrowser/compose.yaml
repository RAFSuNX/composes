services:
  filebrowser:
    hostname: filebrowser
    image: filebrowser/filebrowser:latest
    environment:
      - PUID=1001
      - PGID=1001
      - TZ=${TZ}
    volumes:
      - ${HOME_DIR}:/srv
      - ${HOME_DIR}/docker/filebrowser/database.db:/database.db
      - ${HOME_DIR}/docker/filebrowser/.filebrowser.json:/.filebrowser.json
    networks:
      cloudflared:
        aliases:
          - filebrowser
    restart: always
    # ports:
    #   - "8080:80"

networks:
  cloudflared:
    external: true
